name: spotify_analytics
version: "1.0"
config-version: 2

profile: spotify_analytics

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  spotify_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
